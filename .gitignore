node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
dev-dist
.claude

# Large source brief (kept locally, not in repo)
Create a GIS project plan consisting of three main cases.docx

# Controlled source deck for the PPE zone map (kept locally, not in repo)
zones ppe.pptx

# Controlled source manual for the commissioning tasks (kept locally, not in repo)
CSD Commissioning Manual 2019.pdf
